# Private data directory

Robot descriptions, CAD meshes, collected datasets, checkpoints, and experiment
outputs are intentionally excluded from version control.

The simulation expects the private robot assets to be restored locally under
`data/my_robot/`. Do not commit files from that directory to the public repository.
