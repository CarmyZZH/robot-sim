# 论文方向：以形态上下文 Phi 串联分层控制

## 1. 核心命题

本文研究的不是“用 GNN 拟合一个标量到七个关节角度”，而是：

> 如何用统一、可量化的形态上下文，在任务决策、运动控制和关节执行之间传递机器人当前及目标形态，使可变形机器人能够同时规划运动与形变，并在不同形态下保持稳定控制。

当前机器人只有一条预先设计的连续形变路径，因此形态上下文可以退化为标量：

`phi in [0, 3]`

它是这条一维形态流形上的坐标，而不是通用意义上的完整机器人拓扑。

## 2. 术语边界

- 如果模块连接关系没有断开、重连，建议使用“连续可变形机器人”或“可重构机器人”，不要把每次形变都称为“拓扑变化”。
- `phi_target`：大脑规划的目标形态。
- `phi_hat`：由编码器、IMU 和关节状态估计出的实际形态。
- `z_phi`：未来推广到多机器人或多结构时使用的高维形态表示。
- `G`：模块连接图。只有连接关系变化时，图拓扑才真正变化。

一种可扩展定义是：

`z_phi = Encoder(G, q_morph, lock_state, geometry, dynamics)`

其中 `q_morph` 是变形关节状态，`lock_state` 是锁止约束，`geometry` 是尺寸和轮位，`dynamics` 包含质量、摩擦、电机延迟等参数。

## 3. 三层闭环

### 大脑：任务与形态规划

输入环境观测、任务目标和当前 `phi_hat`，联合输出局部参考轨迹与 `phi_target`。这里回答“去哪里、何时变形、变成什么形态”。

### 小脑：形态条件控制

MPC 的预测模型、约束、轮位和控制权重显式依赖 `phi_hat`。这里回答“在当前形态和形变过程中，怎样稳定跟踪轨迹”。

### 脊髓：约束满足与执行

将 `phi_target` 解码为关节参考，执行锁止约束、局部反馈和故障补偿。这里回答“怎样把目标形态可靠地变成真实关节动作”。

信息流必须是闭环：大脑向下发送目标形态，编码器和底层传感器向上反馈实际形态。仅有单向标量命令不足以支撑实机论文。

## 4. 可投稿的科学问题

主问题：统一形态上下文是否能让分层控制器在连续形变期间，比离散模式切换或各形态独立控制器获得更高的任务成功率、跟踪精度和跨形态泛化能力？

建议验证三个假设：

1. 连续 `phi` 比离散形态 ID 减少切换冲击并提高过渡阶段跟踪精度。
2. 让大脑、小脑、脊髓共享同一形态上下文，比只在底层使用 `phi` 更稳定。
3. 学习或估计实际 `phi_hat/z_phi`，比直接把目标 `phi_target` 当作真实形态更抗负载、摩擦、延迟和关节故障。

## 5. 必要基线

- 表示：离散形态 ID、手工标量 `phi`、手工高维描述、学习得到的 `z_phi`。
- 架构：仅脊髓使用 Phi、脊髓+小脑使用 Phi、三层全部使用 Phi。
- 控制：每形态独立控制器、离散增益调度、连续 Phi 条件 MPC、形态与运动联合规划。
- 解码：解析公式、查表/样条、MLP、GCN、GATv2。
- 反馈：使用 `phi_target`、编码器估计 `phi_hat`、历史观测推断的潜变量。

## 6. 当前实现与论文目标的差距

- 当前 `phi` 是人工定义的一维命令，还不是由传感器估计的状态。
- 当前 GNN 使用固定七节点链，没有展示跨图拓扑泛化。
- 当前标签来自解析公式，不能证明 GNN 比公式或样条更必要。
- 当前 MPC 主要使用统一运动学模型，尚未系统地以 Phi 调节动力学、约束和不确定性。
- 当前上层规则/PPO输出目标 Phi，但缺少实际 Phi 的闭环反馈和实机验证。

## 7. 推荐主线

短期以标量 Phi 做完整实机闭环，论文定位为“连续形态上下文驱动的分层运动-形变协同控制”。

中期将 `phi` 扩展为 `z_phi`，学习不同形态下的残差动力学，并用于形态条件 MPC。只有当数据包含不同连接图、不同故障结构或不同机器人时，再把“跨拓扑泛化”作为核心贡献。
